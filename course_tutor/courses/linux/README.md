# Linux Course

This directory will host course content for the Linux course. The current course data and generated JSON live in `course_tutor/Data/` and `course_tutor/backend/` database migrations.

When migrating content files into this directory, preserve database-driven content: do not duplicate records if they are already stored in the database.
