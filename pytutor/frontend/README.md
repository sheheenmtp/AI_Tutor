# PyTutor Frontend Shim


The canonical frontend sources remain at `pytutor/legacy_py_tutor/frontend/`.

To run the frontend, continue using the original directory:

```bash
cd pytutor/legacy_py_tutor/frontend
npm install
npm run dev
```

This `pytutor/frontend` folder is intentionally a placeholder for the migration.
