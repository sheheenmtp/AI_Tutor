"""Shim package for gradual migration from `py_tutor` to `pytutor`.

This package provides lightweight wrappers that delegate to the existing
`py_tutor` implementation. It avoids duplicating source while allowing
tools and documentation to reference `pytutor` during migration.
"""
